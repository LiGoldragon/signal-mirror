# signal-mirror

Ordinary working wire contract for the sema version-control mirror: the
typed operations a component-side shipper and restorer speak to the
payload-blind mirror daemon. Schema-derived; see `ARCHITECTURE.md`.
